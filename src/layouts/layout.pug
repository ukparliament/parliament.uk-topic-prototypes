doctype html
html(lang="en")
  include ../../src/elements/head.pug
  body
    script.
      document.body.className = ((document.body.className) ? document.body.className + ' has-js' : 'has-js');
    include ../../src/components/header.pug
    main
      block content
    include ../../src/components/footer.pug
    script(src="/javascripts/overrides.js")
    block script
